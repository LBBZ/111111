# .llm Usage

Single import rule:
- Import .llm/AGENT_TOTAL_RULE.md only.
- Other .llm files are optional detailed supplements.

Single upload rule (for external agent context handoff):
- Upload .llm/DEV_AGENT_NAV.md only.

Authority order:
1. .llm/AGENT_TOTAL_RULE.md
2. Supporting files in .llm/

Maintenance:
- This directory is for development-agent project context, not navigation policy prompting.
- Update AGENT_TOTAL_RULE.md first for runtime/architecture changes, then sync supporting docs.
